using UnityEngine;

public class PredictiveTargeting : MonoBehaviour
{
    public static PredictiveTargeting Instance;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
